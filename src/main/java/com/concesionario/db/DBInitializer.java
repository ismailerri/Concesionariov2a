package com.concesionario.db;

// Clase simplificada para inicialización de bases de datos
public class DBInitializer {

    // Constructor simplificado
    public DBInitializer() {
        System.out.println("Modo desarrollo: No se inicializan bases de datos reales");
    }

    // Método vacío (se implementará cuando sea necesario usar SQL)
    private void inicializarSQL() {
        // No hace nada en modo desarrollo
    }

    // Método vacío (se implementará cuando sea necesario usar MongoDB)
    private void inicializarMongoDB() {
        // No hace nada en modo desarrollo
    }
}